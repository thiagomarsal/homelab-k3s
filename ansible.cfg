[defaults]
roles_path = roles
inventory = inventory/hosts.yml
private_key_file = ~/.ssh/id_k3s
remote_user = tfarias
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
